#pragma once

#include <string>

namespace UserData {
std::string BaseDirectoryPath();
std::string DatabaseFilePath();
std::string AudioDirectoryPath();
}

namespace TestData {
std::string BaseDirectoryPath();
std::string DatabaseFilePath();
std::string AudioDirectoryPath();
}
